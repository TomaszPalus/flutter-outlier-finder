name: outlier_app
description: Outlier Finder Flutter application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # Dependency Injection
  get_it: ^7.6.7

  # Internationalization
  flutter_localizations:
    sdk: flutter

  # iOS-style icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
